export function renderSettings(state){return`<header class="page-header"><div><p class="eyebrow">Preferences</p><h1>Settings</h1></div></header><form id="settings-form" class="card" style="max-width:560px"><div class="field"><label>Default rest time</label><select name="rest" class="input">${[30,60,90,120,180].map(n=>`<option value="${n}" ${state.settings.defaultRest===n?'selected':''}>${n} seconds</option>`).join('')}</select></div><div class="form-actions"><button class="btn btn-primary">Save settings</button></div></form>`}
export function bindSettings(state,save,rerender){document.querySelector('#settings-form').onsubmit=e=>{e.preventDefault();state.settings.defaultRest=+new FormData(e.target).get('rest');save();rerender();};}
