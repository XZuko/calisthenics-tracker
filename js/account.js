import{escapeHTML}from'./ui.js';
export function renderAccount({configured,user,sync}){if(!configured)return `<header class="page-header"><div><p class="eyebrow">Cloud backup</p><h1>Account</h1><p class="subtitle">Keep your training data safe and available across devices.</p></div></header><section class="card account-card"><h2>Connect Supabase</h2><p class="subtitle">Add your project URL and public key in <code>js/config.js</code>, then run <code>supabase/schema.sql</code> in the Supabase SQL Editor.</p></section>`;if(user)return `<header class="page-header"><div><p class="eyebrow">Cloud backup</p><h1>Account</h1><p class="subtitle">Your progress is available anywhere you sign in.</p></div></header><section class="card account-card"><span class="sync-badge sync-${sync.value}">${escapeHTML(sync.message||sync.value)}</span><h2>${escapeHTML(user.email)}</h2><p class="subtitle">Changes are saved locally first and backed up automatically.</p><div class="account-actions"><button class="btn" id="sync-now">Sync now</button><button class="btn btn-ghost" id="sign-out">Sign out</button></div></section>`;return `<header class="page-header"><div><p class="eyebrow">Cloud backup</p><h1>Account</h1><p class="subtitle">Sign in to back up your progress and restore it on other devices.</p></div></header><section class="card account-card"><div class="auth-tabs"><button class="btn btn-small auth-tab active" data-mode="signin">Sign in</button><button class="btn btn-small auth-tab" data-mode="signup">Create account</button></div><form id="auth-form" class="auth-form"><div class="field"><label>Email</label><input class="input" type="email" name="email" autocomplete="email" required></div><div class="field"><label>Password</label><input class="input" type="password" name="password" autocomplete="current-password" minlength="6" required></div><p class="auth-error" role="alert"></p><button class="btn btn-primary" id="auth-submit">Sign in</button></form></section>`;}
export function bindAccount(actions){let mode='signin';document.querySelectorAll('.auth-tab').forEach(button=>button.onclick=()=>{mode=button.dataset.mode;document.querySelectorAll('.auth-tab').forEach(x=>x.classList.toggle('active',x===button));document.querySelector('#auth-submit').textContent=mode==='signin'?'Sign in':'Create account';document.querySelector('[name=password]').autocomplete=mode==='signin'?'current-password':'new-password';});const form=document.querySelector('#auth-form');if(form)form.onsubmit=async event=>{event.preventDefault();const error=form.querySelector('.auth-error'),button=form.querySelector('#auth-submit'),data=new FormData(form);error.textContent='';button.disabled=true;try{await actions[mode](data.get('email').trim(),data.get('password'));}catch(err){error.textContent=err.message;}finally{button.disabled=false;}};const sync=document.querySelector('#sync-now');if(sync)sync.onclick=actions.sync;const out=document.querySelector('#sign-out');if(out)out.onclick=actions.signout;}